import { ActionControlDirective } from './action-control.directive';

describe('ActionControlDirective', () => {
  it('should create an instance', () => {
    const directive = new ActionControlDirective();
    expect(directive).toBeTruthy();
  });
});
